<div class="navbar bg-orange-600">
  <div class="flex-1">
    <%= link_to t('header.title'), root_path, class: 'btn btn-ghost text-xl' %>
  </div>
  <div class="space-x-4">
    <%= link_to t('header.list'), "#", class: 'btn btn-neutral' %>
    <%= link_to t('header.post'), "#", class: 'btn btn-neutral' %>
    <details class="dropdown dropdown-end">
      <summary class="btn">open or close</summary>
      <ul class="menu dropdown-content bg-base-100 rounded-box z-[1] w-52 p-2 shadow">
        <li>
          <%= link_to t('header.my_page'), "#" %>
        </li>
        <li>
          <%= link_to t('header.logout'), logout_path, data: { turbo_method: :delete } %>
        </li>
      </ul>
    </details>
  </div>
</div>